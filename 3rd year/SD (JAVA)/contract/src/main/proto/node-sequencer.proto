syntax = "proto3";

package pt.tecnico.blockchainist.contract;

import "common.proto";

// The following option causes top-level messages, enums, and services to be
// defined at the package level, rather than inside an outer class named after
// the .proto file.
option java_multiple_files = true;

message BroadcastRequest {
  Transaction transaction = 1;
}

message Block {
  int32 block_id = 1;
  repeated Transaction transactions = 2;
}

message BroadcastResponse {}

message DeliverBlockRequest {
  int32 block_id = 1;
}

message DeliverBlockResponse {
  Block block = 1;
  double avgTime = 2;
}


message SignedBlockResponse {
  DeliverBlockResponse blockResponse = 1;
  Signature signature = 2;  
}

service SequencerService {
  rpc Broadcast(BroadcastRequest) returns (BroadcastResponse);
  rpc DeliverBlock(DeliverBlockRequest) returns (SignedBlockResponse);
}
